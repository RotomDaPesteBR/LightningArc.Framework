# CLAUDE.md is auto-generated from AGENTS.md by the pre-commit hook (.githooks/pre-commit).
# It must stay tracked so a fresh clone has it, but its diffs are just noise —
# collapse them on GitHub and hide them from CLI diffs.
CLAUDE.md linguist-generated=true
CLAUDE.md -diff
