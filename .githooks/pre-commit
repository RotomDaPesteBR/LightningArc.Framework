#!/bin/sh
# Keeps CLAUDE.md as an exact copy of AGENTS.md.
# AGENTS.md is the canonical instructions file for AI coding agents;
# CLAUDE.md exists only because Claude Code doesn't read AGENTS.md natively.

if [ -f AGENTS.md ]; then
  cp AGENTS.md CLAUDE.md
  git add CLAUDE.md
fi
